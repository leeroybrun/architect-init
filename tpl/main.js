function setup(options, imports, register) {
    // Your plugin code here

    register(null, {
        '--pluginName--': {
            // Register data/objects here
        }
    });
}

module.exports = setup;